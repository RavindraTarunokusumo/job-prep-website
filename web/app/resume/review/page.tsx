import { PlaceholderPage } from "@/components/placeholder-page";

export default function ResumeReviewPage() {
  return (
    <PlaceholderPage
      title="Resume Review"
      description="AI-powered CV checker — ATS risks, structural feedback, and editable rewrites."
      homeHref="/dashboard"
      homeLabel="Back to dashboard"
    />
  );
}