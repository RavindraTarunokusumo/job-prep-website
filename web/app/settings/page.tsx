import { PlaceholderPage } from "@/components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Account preferences, privacy controls, and data export or deletion requests."
      homeHref="/dashboard"
      homeLabel="Back to dashboard"
    />
  );
}