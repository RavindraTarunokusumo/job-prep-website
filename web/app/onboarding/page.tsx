import { PlaceholderPage } from "@/components/placeholder-page";

export default function OnboardingPage() {
  return (
    <PlaceholderPage
      title="Onboarding"
      description="Set your career goals — target role, industry, location, and experience level."
      homeHref="/dashboard"
      homeLabel="Back to dashboard"
    />
  );
}