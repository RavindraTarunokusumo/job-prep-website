import { PlaceholderPage } from "@/components/placeholder-page";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="Your home workspace — readiness score, current plan, and next actions in one view."
      homeHref="/"
      homeLabel="Back to home"
    />
  );
}