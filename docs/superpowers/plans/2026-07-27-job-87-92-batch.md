# Lightweight plan: JOB-87…92 batch

Build order after JOB-85/86: 87 score → 88 gap interview → 89 outcomes → 90 routing → 91 entitlements → 92 journey tests.
Each produces pure functions + prisma models + vitest; UI polish deferred where not required for acceptance of domain contracts.
